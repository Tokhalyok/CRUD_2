# CRUD Application

Полнофункциональное CRUD приложение для управления товарами на Angular + .NET 9 + PostgreSQL.

## Быстрый старт

### Требования
- Node.js 20+
- .NET 9 SDK
- PostgreSQL 14+ (должен быть запущен)

### Установка и запуск (2 минуты)

1. **Клонируйте проект:**
```bash
git clone https://github.com/Tokhalyok/CRUD_2.git
cd CRUD_2
```

2. **⚠️ Убедитесь что PostgreSQL запущен:**
- Windows: Services > PostgreSQL > Start
- Linux: `sudo systemctl start postgresql`
- macOS: `brew services start postgresql`

3. **Создайте БД (если ещё не создана):**
```bash
cd ProductApi
psql -U postgres -c "CREATE DATABASE productdb;"
```

4. **Установите зависимости:**
```bash
# Бэкенд
cd ProductApi
dotnet restore

# Фронтенд
cd ../product-client
npm install
```

5. **Примените миграции БД:**
```bash
cd ProductApi
dotnet ef database update
```

6. **Запустите в двух терминалах:**

**Терминал 1 - Бэкенд (.NET):**
```bash
cd ProductApi
dotnet run
```
API будет на http://localhost:5231

**Терминал 2 - Фронтенд (Angular):**
```bash
cd product-client
ng serve --port 4201
```
Приложение откроется на http://localhost:4201

## Возможности

- ✅ **Добавить товар** - интуитивная форма с валидацией
- ✅ **Просмотреть товары** - красивая таблица со всеми товарами
- ✅ **Редактировать товар** - нажмите на кнопку ✏️ для редактирования
- ✅ **Удалить товар** - удаляйте товары с подтверждением 🗑️

## Структура

```
CRUD_2/
├── ProductApi/                 # Бэкенд (.NET 9)
│   ├── Controllers/
│   ├── Models/
│   ├── Data/
│   ├── Migrations/
│   ├── Program.cs
│   └── ProductApi.csproj
│
└── product-client/             # Фронтенд (Angular 19)
    ├── src/
    │   ├── app/
    │   │   ├── components/     # Компоненты (Form, List)
    │   │   ├── services/       # ProductService с BehaviorSubject
    │   │   ├── models/         # Интерфейсы (Product)
    │   │   ├── app.ts          # Главный компонент
    │   │   ├── app.config.ts   # Конфигурация приложения
    │   │   └── app.routes.ts   # Роуты
    │   ├── index.html          # HTML шаблон
    │   ├── main.ts             # Точка входа
    │   └── styles.scss         # Глобальные стили
    ├── package.json
    ├── angular.json
    └── tsconfig.json
```

## Технологии

- **Frontend:** 
  - Angular 19 (Standalone Components)
  - TypeScript
  - RxJS (BehaviorSubject для управления состоянием)
  - FormsModule для двусторонней привязки
  - HttpClient для API запросов

- **Backend:** 
  - .NET 9
  - ASP.NET Core
  - Entity Framework Core
  - PostgreSQL

- **API:** RESTful с полной поддержкой CRUD

## Архитектура

### Frontend
- **Shared Service Pattern**: `ProductService` управляет состоянием всех товаров через `BehaviorSubject`
- **Component Communication**: Компоненты взаимодействуют через сервис без прямых зависимостей
- **Reactive Programming**: Используется RxJS для работы с потоками данных
- **Standalone Components**: Современный подход Angular 19

### Backend
- **RESTful API**: Стандартные HTTP методы (GET, POST, PUT, DELETE)
- **Entity Framework Core**: ORM для работы с БД
- **Migration System**: Контролируемое управление схемой БД

## Помощь

### Если не запускается:
1. **Убедитесь что PostgreSQL запущен** - это критично!
2. **Проверьте что порты 5231 и 4201 свободны** (или используются порты, показанные в выводе)
3. **Установите все зависимости:**
   ```bash
   npm install     # для фронтенда
   dotnet restore  # для бэкенда
   ```
4. **Примените миграции БД:**
   ```bash
   cd ProductApi
   dotnet ef database update
   ```

### Типичные ошибки:

**Ошибка подключения к БД:**
- Проверьте что PostgreSQL запущен
- Проверьте учетные данные в `appsettings.json`
- Убедитесь что база данных `productdb` создана

**Angular не компилируется:**
- Удалите `node_modules` и переустановите: `npm install`
- Очистите кэш: `ng cache clean`

**API не отвечает:**
- Проверьте что бэкенд запущен на `localhost:5231`
- Смотрите логи консоли для ошибок

## Скриншоты функциональности

### Форма добавления товара
- Валидация всех полей
- Сообщения об ошибках и успехе
- Кнопка "Сохранить" / "Добавить" в зависимости от режима
- Кнопка "Отмена" при редактировании

### Список товаров
- Таблица со всеми товарами
- Кнопка ✏️ для редактирования товара
- Кнопка 🗑️ для удаления с подтверждением
- Красивая подсветка при наведении
- Счётчик всех товаров

## Лицензия

MIT
