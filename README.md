# CRUD Application

Полнофункциональное CRUD приложение для управления товарами на Angular + .NET 9 + PostgreSQL.

## Быстрый старт

### Требования
- Node.js 20+
- .NET 9 SDK
- PostgreSQL 14+

### Установка и запуск (2 минуты)

1. **Клонируйте проект:**
```bash
git clone https://github.com/Tokhalyok/CRUD_2.git
cd CRUD_2
```

2. **Установите зависимости:**
```bash
# Бэкенд
cd ProductApi
dotnet restore

# Фронтенд
cd ../product-client
npm install
```

3. **Примените миграции БД:**
```bash
cd ProductApi
dotnet ef database update
```

4. **Запустите в двух терминалах:**

**Терминал 1 - Бэкенд (.NET):**
```bash
cd ProductApi
dotnet run
```
API будет на http://localhost:5231

**Терминал 2 - Фронтенд (Angular):**
```bash
cd product-client
ng serve --port 4201
```
Приложение откроется на http://localhost:4201

## Возможности

- ➕ Добавить товар
- 📋 Просмотреть все товары
- ✏️ Редактировать товар
- ❌ Удалить товар

## Структура

```
CRUD_2/
├── ProductApi/           # Бэкенд (.NET Core 9)
│   ├── Controllers/
│   ├── Models/
│   ├── Data/
│   └── Program.cs
└── product-client/       # Фронтенд (Angular 19)
    ├── src/
    │   ├── app/
    │   │   ├── components/
    │   │   ├── models/
    │   │   └── services/
    │   └── main.ts
    └── package.json
```

## Технологии

- **Frontend:** Angular 19, TypeScript, Standalone Components
- **Backend:** .NET 9, Entity Framework Core, ASP.NET Core
- **Database:** PostgreSQL
- **API:** RESTful

## Помощь

Если не запускается:
1. Убедитесь что PostgreSQL запущен
2. Проверьте что порты 5231 и 4201 свободны
3. Установите все зависимости (`npm install`, `dotnet restore`)
